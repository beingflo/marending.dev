---
title: 'Towards approachable observability with wide events'
subtitle: 'A tragedy in four acts'
date: '17 May 2026'
link: '/notes/wide-events/'
layout: 'src/layouts/NoteLayout.astro'
draft: true
---

I like to think about how to get operational insights into services I host. It's become kind of a
hobby to be honest. Maybe this whole thing is just bike shedding. What better way to avoid the
crushing reality that an actual project you had in mind doesn't live up to your expectations than by
not even attempting it and instead building infrastructure around your existing applications. Maybe
that's a topic for therapy.

Anyhow, I realized during my latest manic observability episode that I barely even remember how I
got here. So for posterity, I'm outlining the history and motivations for all my different phases in
this note.

## Act 1: Metrics and Prometheus

A couple of years ago I got the itch to play around with embedded devices. I got myself an ESP32 and
a CO2 sensor and got to work. With a little ellbow grease, I had the current CO2 concentration
displayed on a small seven segment display. Quickly, I graduated to wanting to store and chart this
data.

At this point I had little knowledge on how to approach this, so I went the well-trodden path.
Prometheus to store time series data and Grafana to visualize it. Since my microcontroller was not
internet accessible, I had to put a [push gateway](https://github.com/prometheus/pushgateway) in
front of Prometheus to allow pushing data to the system, instead of having Prometheus scrape the
metrics off the device regularly.

Around this time I also built and deployed this website. I had seen a cool statistics section on
another blog, where one could view how many page views the blog was getting and which pages were
popular. Wanting the same for this site, I initially transformed the static site into one with a
NodeJS backend just for this specific feature. After a short while I realized that I'm not happy
with that and ripped it out again, planning on tracking visitors in an external service. You can
watch this unfold in [this old note](/notes/performance/).

Now I could have used my existing Prometheus setup to scrape Caddy for access metrics, but that
didn't give me all the data I would have liked. I would need to have
[dedicated service serving the site](/notes/rust-spa/) and exposing metrics. So this kicked off the
thought process that I need a broader observability system than what Prometheus can give me. What
was always clear to me, by the way, is that I wanted one unified system for metrics and service
monitoring.

Thinking about this in hindsight, I also realize that there was another push factor: PromQL. I got
annoyed with learning a language with such a narrow use case.

## Act 2: Metrics with DuckDB

With all this in place, the time was ripe to venture out. And boy did I. The requirements for my
next system were clear: It should accept arbitrary JSON payloads (like sensor readings, or access
logs), store them efficiently and allow querying them in an ergonomical fashion.

Notice the shift away from time series data to something more general, something that could cover
multiple use cases. You can follow the journey from
[musing on how to store that data](/notes/unstructured-data/) to
[benchmarking suitable databases](/notes/sqlite-vs-duckdb/) in my notes.

Coming away from PromQL, I was craving more expressive power for querying and transforming data. So
I started looking into building [a WebAssembly plugin system](/notes/wasm-benchmark/) to safely
execute efficient transformations server-side. For visualization, I was not satisfied with Grafana
anymore, I wanted to go custom as well. So I built a SolidJS frontend with
[responsive plots](/notes/responsive-plots/) using Observable Plot. All this started taking shape
under the name [observatory](https://github.com/beingflo/observatory).

![charts](./charts.jpg)

You might already be guessing that this would not go so well. Indeed, the sensor readings and also
GPS location data from my phone were handled fine, but I was missing a puzzle piece on how access
logs fit in. How would I instrument my services in such a way as to get the data into observatory in
a queryable form? I found the answer to that question in the form of
[traces](https://opentelemetry.io/docs/concepts/signals/traces/). At first I wanted to build a
tracing collector into observatory, but that proved to be difficult. Instead, I turned towards a
more off-the-shelf stack to ingest tracing data.

![location](./location.jpg)

## Act 3: Tracing with ClickHouse

When I learned about tracing - not just in the context of distributed systems - but the general
concept of spans and events, I felt like I had hit the jackpot. Clearly, events you emit within
spans mimick log lines, except they are naturally associated with a particular request. No
correlation id needed (i.e. it's the span id and the tooling handles it for you). Metrics are just
hardcoded aggregations you do in the application which lends itself to very space-efficient storage
in exchange for limited flexibility. With traces, you can generate any metrics you might care about
after the fact.

So I set up an OpenTelemetry collector that would receive traces and store them in a
[ClickHouse](/notes/clickhouse/) database. That part made a lot of sense, and instrumenting my
services with the excellent `tracing` crate is a walk in the park. Getting that sent off
[not so much](/notes/otel/), though. But now the sensor and GPS data didn't fit the bill so cleanly
anymore. I didn't want to have to send that data in a format the collector would understand, so I
just built a small service I called [events](https://github.com/beingflo/events), that accepts that
data and then simply declares a span with the received attributes. This way, the tracing machinery
in _that_ service makes sure to get that data into ClickHouse (via the collector).

So with this, I finally realized the dream of having one central system where all my data flows
together:

![grafana screenshot](./grafana.png) ![grafana screenshot](./access-logs.png)
![grafana screenshot](./location-grafana.jpg)

So this is great. Just a couple of problems. I don't like OpenTelemetry because I don't understand
it.

If this sounds hacky, that's because it is. Even small sensor readings that could be stored in,
let's say, eight bytes or 16 bytes, blow up to a full database row of a rather complicated database
schema needed for open telemetry tracing. But the simplicity of it is undeniable.

But now at the time of writing, here I am again looking to simplify. The motivating factor here is
that I am about to migrate my virtual private server from a Headsner machine to a MacBook Air living
in my closet. So I'm starting to be very energy conscious. Looking at my utilization on my Headsner
server, click house is constantly pegging the CPU at 40%. This was the initial reason I was once
again looking towards a serverless model. An embedded system, an embedded database that causes no
load on the system when there are no queries sound very appealing.

## Act 4: Wide events with Parquet

-> events v2

[blog](https://jeremymorrell.dev/blog/a-practitioners-guide-to-wide-events/)

So I have been doing some reading again. And this time I've stumbled across the next interesting
concept. Just like I did with tracing in the last iteration, this time it is wide events. The idea
is very simple. As a backend system is working towards responding to a request, you build up
properties in a context that you can ultimately at the time you respond to the request collect into
a wide event and send towards the observability system. One thing to note is that this is
significantly simpler than tracing. There are no sophisticated protocols, nothing. It's a simple
chase and blob. The second thing to note is that you can still use this to generate any metrics that
you would like. You can count requests per second, you can aggregate errors per user agent, whatever
you want. You have all the data you could possibly want in these events.

If the query engine is performant enough to pull out arbitrary fields from these JSON objects, we
can put whatever data we want into that system and get nice dashboards. For instance, the IoT data
that I want to ingest will also be simple JSON blocks that I store. The GPS data from my phone, the
same.

If you haven't already, you should check out the last note where you can read a bunch of my thought
process towards this new system. To summarize, the idea is to radically simplify from an active
database down to simple parquet files. At this point, I'm planning to use DocDB as the writer as
well as the query engine to read and write those files. But ultimately, the docdb could easily be
switched out for a different query engine at any point. The simplicity of the system is that there
would be a single Rost service that accepts arbitrary JSOS and writes this directly to a parquet
file. [parquet](/notes/duckdb-parquet/)
