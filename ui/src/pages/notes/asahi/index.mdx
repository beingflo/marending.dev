---
title: 'Asahi Linux on a Macbook Air M1'
subtitle: 'Setting up a headless server'
date: '25 Jun 2026'
link: '/notes/asahi/'
layout: 'src/layouts/NoteLayout.astro'
draft: true
---

Install Asahi via script

Terminfo for Ghostty

`infocmp -x xterm-ghostty | ssh YOUR-SERVER -- tic -x -`

Disable cockpit

`sudo systemctl disable --now cockpit.socket`

Ensure screen goes off

`sudo grubby --update-kernel=ALL --args="consoleblank=30"`

Set battery charget to max 80%

`echo 'SUBSYSTEM=="power_supply", KERNEL=="macsmc-battery", ATTR{charge_control_end_threshold}="80"' | sudo tee /etc/udev/rules.d/10-battery.rules`
