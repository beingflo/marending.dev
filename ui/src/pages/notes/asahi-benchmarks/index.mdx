---
title: 'Apple M1 vs Hetzner CAX21'
subtitle: 'Benchmarking homeserver hardware'
date: '08 Aug 2026'
link: '/notes/asahi-benchmarks/'
layout: 'src/layouts/NoteLayout.astro'
draft: true
---

import CPUResults from './CPUResults.astro';
import MemoryResults from './MemoryResults.astro';
import FileIOResults from './FileIOResults.astro';
import RustCompile from './RustCompile.astro';

I'm in the process of migrating my VPS over to a [MacBook Air M1 running Asahi](/notes/asahi-setup/)
in my closet. Before I decommission the VPS though, I'm curious to see what the performance
difference is. So here are a couple of non-representative benchmarks comparing the M1 to an Ampere-
based CAX21 at Hetzner.

By the way, I've already benchmarked these Arm-based Hetzner machines before.
[Last time](notes/vps-benchmarks/), I was in the process of migrating from an x86 machine to the
CAX31. In the meantime, I've downgraded to the CAX21, that's why these numbers don't exactly match
up.

Anyway, here are the specs of the two machines:

CAX21: **4 vCPU, 8 GB RAM**

M1: **4 performance cores, 4 efficiency cores, 16 GB RAM**

Note that we're comparing a VPS with noisy neighbors and modest production load to an idle M1. Also,
the tests are rather short, so the impact of thermal throttling on the passively cooled M1 is not
visible. I did say those will be non-representative benchmarks.

```
sysbench cpu --threads=x run
```

<CPUResults />

```
sysbench memory --threads=x run
```

<MemoryResults />

```
sysbench fileio --threads=x --file-test-mode=seqwr run
```

<FileIOResults />

And finally I wanted to see how they compare when compiling one of my Rust code bases.

```
time cargo build --release
```

<RustCompile />

## Verdict

It's simultaneously unsurprising that a dedicated M1 is more performance than a tiny slice of a server
CPU, and yet I'm always blown away by what this little 6-year-old machine can do. Needless to say, this
migration will be a nice little upgrade. Although I'm more excited about the energy efficiency of
the Air than the raw power.
